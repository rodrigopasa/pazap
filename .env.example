# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# Alternative PostgreSQL variables (if DATABASE_URL is not available)
PGUSER=postgres
PGPASSWORD=your_password
PGHOST=localhost
PGPORT=5432
PGDATABASE=whatsapp_system

# Application Configuration
NODE_ENV=production
PORT=5000

# Session Configuration
SESSION_SECRET=your_very_secure_session_secret_here

# Optional: WhatsApp Service Configuration
# Add any API keys or service credentials needed for WhatsApp integration